package com.techelevator;

public class VariableNames {

    /*
    Class:
    CateringItem
        -priceOfItem
        -itemName
        -itemCode
        -itemType

    InventoryFileReader
        -fileName (this holds the csv file)
        -file (we passed fileName into this variable)
        -itemList (the array of all of the items)
        -fileScanner (whats reading our "file")
        -itemDetails (our String array that SPLIT the "file" \\|)
        -items (the array of items)

    CateringSystem
        -inventoryMap (variable that holds the inventory Map from the FileReader)





















     */
}
